@model IEnumerable<StoresManagement.ViewModels.BranchFormViewModel>

@{
    ViewData["Title"] = "List Branches";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>List of the branches in this entity</h1>

@if (!Model.Any())
{
    <p>We don't have any branches yet.</p>
}
else
{

    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Entity)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Identification)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Address)
                </th>
                <th>See the producs of the branch: </th>
                <th>See the purchases of the branch: </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Entity.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Identification)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Contact.Address)
                    </td>
                    <td>
                        <a asp-controller="Products" asp-action="ListProductsByBranch" asp-route-id="@item.Id">List products</a>
                    </td>
                    <td>
                        <a asp-controller="Purchases" asp-action="ListPurchasesByBranch" asp-route-id="@item.Id">List Purchases</a>
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}