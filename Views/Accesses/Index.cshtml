@model IEnumerable<StoresManagement.ViewModels.OperatorFormViewModel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Register">Register New User</a>
</p>

<head>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <link href="https://unpkg.com/tabulator-tables@4.2.7/dist/css/tabulator.min.css" rel="stylesheet">
    <script type="text/javascript" src="https://unpkg.com/tabulator-tables@4.2.7/dist/js/tabulator.min.js"></script>
</head>

<body>
    <div class="form-group">
        <div id="index-table"></div>
    </div>
</body>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}

    <script>
        //custom formatter definition
        var actionButtons = function (cell, formatterParams, onRendered) { //plain text value
            let id = cell.getRow().getData().id;

            return "<input type=\"submit\" value=\"Edit\" class=\"btn btn - primary\" style=\"color: black; border: 1px solid gray;\" / onclick=\"editRecord(" + id + ")\"> ";
        };

        function editRecord(id) {
            window.location.href = '@Url.Action("Edit", "Accesses")/' + id;
        }

        $(document).ready(function () {

            //Build Tabulator
            var table = new Tabulator("#index-table", {
                layout: "fitData",
                ajaxURL: "/Accesses/ListAll",
                ajaxProgressiveLoad: "scroll",
                placeholder: "There are no records",
                columns: [
                    { field: "id", visible: false },
                    { title: "Entity", field: "entity", headerFilter: "input" },
                    { title: "UserName", field: "userName", headerFilter: "input" },
                    { title: "Role", field: "role", headerFilter: "input" },
                    { title: "Email", field: "email", headerFilter: "input" },
                    { title: "Phone Number", field: "phonenumber", headerFilter: "input" },
                    { title: "Action", formatter: actionButtons, align: "center" },
                ],
            });

        });
    </script>
}